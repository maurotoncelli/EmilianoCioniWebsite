# LEGACY — hosting migrato su Vercel (vedi vercel.json e MIGRAZIONE_VERCEL.md)

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
